---
// Componente Hero - Sección de introducción
---

<section id="hero" class="min-h-screen flex items-center justify-center px-4 py-20">
    <div class="glass rounded-2xl p-8 md:p-12 w-full">
        <div class="flex flex-col md:flex-row items-center justify-center gap-8 md:gap-16">
            
            <div class="text-center md:text-left order-2 md:order-1 flex-1">
                <h1 class="text-3xl md:text-5xl font-bold mb-4 pb-3 neon-white">
                    <span id="typing-text"></span><span class="typing-cursor">|</span>
                </h1>
                <p class="text-lg md:text-xl mb-3 text-gray-300 font-light">
                    Estudiante de ASIR
                </p>
                <p class="text-base md:text-lg text-gray-400 mb-6 leading-relaxed">
                    Actualmente cursando el segundo año de Administración de Sistemas Informáticos en Red (ASIR). 
                    Con gran ambición y ganas de seguir formándome, mi objetivo es especializarme en el sector de la ciberseguridad. 
                    Apasionado por la tecnología y siempre en constante aprendizaje.
                </p>
                
                <div class="flex flex-col sm:flex-row gap-7 justify-center md:justify-start items-center">
                    <a href="/cv.pdf" download class="cv-button px-8 py-3 rounded-lg font-medium transition-all duration-300 transform hover:scale-105">
                        CV
                    </a>
                    <div class="flex gap-7">
                        <a href="https://github.com/DorianSGT" target="_blank" class="social-icon group">
                            <img src="imagenes/github.png" alt="GitHub" class="w-9 h-9 transition-all duration-300 group-hover:scale-110 white-icon" />
                        </a>
                        <a href="https://www.linkedin.com/in/dorian-garc%C3%ADa-a09402350/" target="_blank" class="social-icon group">
                            <img src="imagenes/linkedin.png" alt="LinkedIn" class="w-9 h-9 transition-all duration-300 group-hover:scale-110 white-icon" />
                        </a>
                    </div>
                </div>
            </div>

            <div class="flex-shrink-0 order-1 md:order-2">
                <div class="avatar-container relative w-40 h-40 md:w-64 md:h-64 rounded-full overflow-hidden group">
                    <img 
                        src="/imagenes/avataar.png" 
                        alt="Dorian García" 
                        class="w-full h-full object-cover block transition-transform duration-500 group-hover:scale-105"
                    />
                </div>
            </div>
            
        </div>
    </div>
</section>

<script>
    const text = "Hola, soy Dorian García";
    const typingElement = document.getElementById('typing-text');
    
    if (typingElement) {
        let index = 0;
        const typingSpeed = 100; 
        function typeText() {
            if (typingElement && index < text.length) {
                typingElement.textContent += text.charAt(index);
                index++;
                setTimeout(typeText, typingSpeed);
            }
        }
        setTimeout(typeText, 500);
    }
</script>

<style>
    /* CURSOR TYPING */
    .typing-cursor {
        display: inline-block;
        animation: blink 1s infinite;
        margin-left: 2px;
        font-weight: 100;
        color: #ffffff;
    }
    
    @keyframes blink {
        0%, 50% { opacity: 1; }
        51%, 100% { opacity: 0; }
    }
    
    /* BOTÓN CV */
    .cv-button {
        background: linear-gradient(135deg, #0ea5e9 20%, #6366f1 50%, #0ea5e9 100%);
        background-size: 200% 200%;
        animation: gradient-shift 3s ease infinite;
        color: white;
        box-shadow: 0 0 15px rgba(14, 165, 233, 0.3);
    }
    
    @keyframes gradient-shift {
        0% { background-position: 0% 50%; }
        50% { background-position: 100% 50%; }
        100% { background-position: 0% 50%; }
    }
    
    .cv-button:hover {
        box-shadow: 0 0 25px rgba(14, 165, 233, 0.6);
    }
    
    /* ICONOS */
    .white-icon {
        filter: brightness(0) invert(1);
        opacity: 0.7; 
        transition: all 0.3s ease;
    }

    .social-icon:hover .white-icon {
        opacity: 1;
        filter: drop-shadow(0 0 8px #7dd4fc5f) brightness(0) invert(1);
        transform: scale(1.1);
    }
    
    /* ------------------------------------------- */
    /* AVATAR: EFECTO SHEEN (SIN BORDES NI DIVS VISIBLES) */
    /* ------------------------------------------- */
    
    .avatar-container {
        /* Esto es vital para que el brillo sea redondo, aunque no veas el div */
        border-radius: 50%; 
        /* Truco para Safari/Chrome para recortar el brillo perfectamente */
        mask-image: radial-gradient(white, black);
        -webkit-mask-image: -webkit-radial-gradient(white, black);
    }

    /* EL BRILLO INVISIBLE QUE APARECE */
    .avatar-container::after {
        content: '';
        position: absolute;
        top: 0;
        left: -150%; 
        width: 100%;
        height: 100%;
        
        background: linear-gradient(
            to right, 
            rgba(255,255,255,0) 0%, 
            rgba(125, 211, 252, 0.6) 50%, 
            rgba(255,255,255,0) 100%
        );
        
        transform: skewX(-30deg);
        pointer-events: none; 
    }

    .avatar-container:hover::after {
        animation: sheen 0.8s ease-in-out forwards;
    }

    @keyframes sheen {
        100% {
            left: 150%;
        }
    }
</style>