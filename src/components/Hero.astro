---
// Componente Hero - Sección de introducción
---

<section
    id="hero"
    class="min-h-screen flex items-center justify-center px-4 pt-24 md:pt-20 pb-10"
>
    <div class="glass rounded-2xl p-6 md:p-12 w-full">
        <div
            class="flex flex-col md:flex-row items-center justify-center gap-6 md:gap-16"
        >
            <div class="text-center md:text-left order-2 md:order-1 flex-1">
                <h1 class="text-2xl md:text-5xl font-bold mb-4 pb-3 neon-white">
                    <span id="typing-text"></span><span class="typing-cursor"
                        >|</span
                    >
                </h1>
                <p class="text-lg md:text-xl mb-3 text-gray-300 font-light">
                    Estudiante de ASIR
                </p>
                <p
                    class="text-sm md:text-lg text-gray-400 mb-6 leading-relaxed"
                >
                    Actualmente cursando el segundo año de Administración de
                    Sistemas Informáticos en Red (ASIR). Con gran ambición y
                    ganas de seguir formándome, mi objetivo es especializarme en
                    el sector de la ciberseguridad. Apasionado por la tecnología
                    y siempre en constante aprendizaje.
                </p>

                <div
                    class="flex flex-col sm:flex-row gap-7 justify-center md:justify-start items-center"
                >
                    <a
                        href="/cv/CVDorianGarcía.pdf"
                        download="CVDorianGarcía.pdf"
                        class="btn-galaxy"
                    >
                        <strong>CV</strong>
                        <div id="container-stars">
                            <div id="stars"></div>
                        </div>
                        <div id="glow">
                            <div class="circle"></div>
                            <div class="circle"></div>
                        </div>
                    </a>

                    <div class="flex gap-7">
                        <a
                            href="https://github.com/DorianSGT"
                            target="_blank"
                            class="social-icon group"
                        >
                            <img
                                src="imagenes/github.png"
                                alt="GitHub"
                                class="w-9 h-9 transition-all duration-300 group-hover:scale-110 white-icon"
                            />
                        </a>
                        <a
                            href="https://www.linkedin.com/in/dorian-garc%C3%ADa-a09402350/"
                            target="_blank"
                            class="social-icon group"
                        >
                            <img
                                src="imagenes/linkedin.png"
                                alt="LinkedIn"
                                class="w-9 h-9 transition-all duration-300 group-hover:scale-110 white-icon"
                            />
                        </a>
                    </div>
                </div>
            </div>

            <div class="flex-shrink-0 order-1 md:order-2">
                <div
                    class="avatar-container relative w-32 h-32 md:w-64 md:h-64 rounded-full overflow-hidden group"
                >
                    <img
                        src="/imagenes/avataar.png"
                        alt="Dorian García"
                        class="w-full h-full object-cover block transition-transform duration-500 group-hover:scale-105"
                    />
                </div>
            </div>
        </div>
    </div>
</section>

<script>
    const text = "Hola, soy Dorian García";
    const typingElement = document.getElementById("typing-text");

    if (typingElement) {
        let index = 0;
        const typingSpeed = 100;
        function typeText() {
            if (typingElement && index < text.length) {
                typingElement.textContent += text.charAt(index);
                index++;
                setTimeout(typeText, typingSpeed);
            }
        }
        setTimeout(typeText, 500);
    }
</script>

<style>
    /* CURSOR TYPING */
    .typing-cursor {
        display: inline-block;
        animation: blink 1s infinite;
        margin-left: 2px;
        font-weight: 100;
        color: #ffffff;
    }

    @keyframes blink {
        0%,
        50% {
            opacity: 1;
        }
        51%,
        100% {
            opacity: 0;
        }
    }

    /* ------------------------------------------- */
    /* BOTÓN GALAXY (Estilo Cyber-Nebulosa)        */
    /* ------------------------------------------- */

    .btn-galaxy {
        /* Configuración base del contenedor */
        display: flex;
        justify-content: center;
        align-items: center;
        width: 6rem;
        height: 3rem;
        overflow: hidden;
        background-size: 300% 300%;
        cursor: pointer;
        backdrop-filter: blur(1rem);
        border-radius: 5rem;
        transition: 0.5s;
        text-decoration: none; /* Quitamos subrayado */

        /* Animación del borde gradiente */
        animation: gradient_301 5s ease infinite;
        border: double 4px transparent;

        /* AQUI ESTÁ LA MAGIA DE LOS COLORES */
        /* Capa 1: Fondo oscuro (Tu nebulosa #1b2735) */
        /* Capa 2: El gradiente del borde (Cyan, Azul, Violeta) */
        background-image:
            linear-gradient(#1b2735, #1b2735),
            linear-gradient(
                137.48deg,
                #2045c8 10%,
                /* Cyan */ #6366f1 45%,
                /* Indigo */ #8f51ea 67%,
                /* Violeta */ #0044ff 87% /* Azul Eléctrico */
            );
        background-origin: border-box;
        background-clip: content-box, border-box;
    }

    /* Contenedor de estrellas */
    #container-stars {
        position: absolute;
        z-index: -1;
        width: 100%;
        height: 100%;
        overflow: hidden;
        transition: 0.5s;
        backdrop-filter: blur(1rem);
        border-radius: 5rem;
        background-color: rgba(5, 7, 10, 0.567);
    }

    /* Texto del botón */
    strong {
        z-index: 2;
        font-family: "Roboto", sans-serif;
        font-size: 16px; /* Un pelín más grande para leerse bien */
        letter-spacing: 3px;
        color: #ffffff;
        text-shadow: 0 0 7px rgba(255, 255, 255, 0.935);
        text-transform: uppercase;
        font-weight: bold;
    }

    /* El brillo interior (Blobs) */
    #glow {
        position: absolute;
        display: flex;
        width: 12rem;
    }

    .circle {
        width: 100%;
        height: 30px;
        filter: blur(2rem);
        animation: pulse_3011 4s infinite;
        z-index: -1;
    }

    /* Colores del brillo interior adaptados a tu tema */
    .circle:nth-of-type(1) {
        background: rgba(14, 165, 233, 0.636); /* Cyan */
    }

    .circle:nth-of-type(2) {
        background: rgba(99, 102, 241, 0.704); /* Indigo */
    }

    /* Interacciones Hover */
    .btn-galaxy:hover #container-stars {
        z-index: 1;
        background-color: #1b2735; /* Fondo nebulosa al hacer hover */
    }

    .btn-galaxy:hover {
        transform: scale(1.05); /* Escala sutil */
    }

    .btn-galaxy:active {
        border: double 4px #0ea5e9; /* Borde cyan al hacer click */
        background-origin: border-box;
        background-clip: content-box, border-box;
        animation: none;
    }

    .btn-galaxy:active .circle {
        background: #0ea5e9;
    }

    /* Animación de estrellas de fondo */
    #stars {
        position: relative;
        background: transparent;
        width: 200rem;
        height: 200rem;
    }

    #stars::after {
        content: "";
        position: absolute;
        top: -10rem;
        left: -100rem;
        width: 100%;
        height: 100%;
        animation: animStarRotate 90s linear infinite;
        background-image: radial-gradient(#ffffff 1px, transparent 1%);
        background-size: 50px 50px;
    }

    #stars::before {
        content: "";
        position: absolute;
        top: 0;
        left: -50%;
        width: 170%;
        height: 500%;
        animation: animStar 60s linear infinite;
        background-image: radial-gradient(#ffffff 1px, transparent 1%);
        background-size: 50px 50px;
        opacity: 0.5;
    }

    /* Keyframes */
    @keyframes animStar {
        from {
            transform: translateY(0);
        }
        to {
            transform: translateY(-135rem);
        }
    }

    @keyframes animStarRotate {
        from {
            transform: rotate(360deg);
        }
        to {
            transform: rotate(0);
        }
    }

    @keyframes gradient_301 {
        0% {
            background-position: 0% 50%;
        }
        50% {
            background-position: 100% 50%;
        }
        100% {
            background-position: 0% 50%;
        }
    }

    @keyframes pulse_3011 {
        0% {
            transform: scale(0.75);
            box-shadow: 0 0 0 0 rgba(14, 165, 233, 0.7);
        }
        70% {
            transform: scale(1);
            box-shadow: 0 0 0 10px rgba(14, 165, 233, 0);
        }
        100% {
            transform: scale(0.75);
            box-shadow: 0 0 0 0 rgba(14, 165, 233, 0);
        }
    }

    /* ICONOS SOCIALES */
    .white-icon {
        filter: brightness(0) invert(1);
        opacity: 0.7;
        transition: all 0.3s ease;
    }

    .social-icon:hover .white-icon {
        opacity: 1;
        filter: drop-shadow(0 0 8px #7dd4fc5f) brightness(0) invert(1);
        transform: scale(1.1);
    }

    /* AVATAR */
    .avatar-container {
        border-radius: 50%;
        mask-image: radial-gradient(white, black);
        -webkit-mask-image: -webkit-radial-gradient(white, black);
    }

    .avatar-container::after {
        content: "";
        position: absolute;
        top: 0;
        left: -150%;
        width: 100%;
        height: 100%;
        background: linear-gradient(
            to right,
            rgba(255, 255, 255, 0) 0%,
            rgba(125, 211, 252, 0.6) 50%,
            rgba(255, 255, 255, 0) 100%
        );
        transform: skewX(-30deg);
        pointer-events: none;
    }

    .avatar-container:hover::after {
        animation: sheen 0.8s ease-in-out forwards;
    }

    @keyframes sheen {
        100% {
            left: 150%;
        }
    }
</style>
